# 0.14.0 validation and review record

## Resume and implementation provenance

Base main893a6152a8ec7d1878d4cf2061dc1b6824542ddf; existing task branch feature/0.14.0-process-observation and PR #36. The prior accepted but uncommitted implementation tree dea3a68eee5229b6cea6f260a7e47f76229373d4 was read and reconciled. The three previous local-only UI/report files were not mounted in the resumed runtime; the UI, timeline and report were reconstructed against the approved contracts. Ordinary connector writes succeeded this session; no bypass, alternate write route, permission expansion, new branch or force push was used. This does not establish the cause or permanent resolution of earlier tool-evaluation failures.

## Observed RED

Test-only af8dda69b3721e4222435429be02dada6f7e5e3f, tested merge5b20e2f8af8588c87f63b5705e69bba4c139292a, Windows EXE34714339296/job103608670011: zero compiler warnings/errors, prior suites passed, process-observation behavior0/33 and integration1/8 on explicit stubs/absent UI. The one passing integration case was absence of the new button in event-log mode, not working observation. Packaging was skipped.

## Observed initial GREEN

Implementation729a0e0cd3a60dc80e9d7455771816e625912b8a, treef627cf3c52d86d09c4755850f998192a9a3c45c4, tested mergea477ccba87ef1a8e260eea1b4037a6d598bb84fa: [Windows EXE34716848751](https://github.com/bajoicheg/g-pc-health-check/actions/runs/34716848751), job103615531998, all original build/source/published-EXE/portable/version/checksum/pilot steps passed. PR artifact upload was skipped by the unchanged existing workflow. These steps include the33 behavior and8 integration contracts and earlier suites. Successful step summaries were inspected; this record does not invent a benchmark or workstation pilot.

## Final completion acceptance

Eight additional checks are added on the same implementation: actual process-view render and known/unknown selection; unique link and busy gating; numeric/idle form semantics; all9 report graphs and encoded raw JSON; painting every process metric plus empty state; exactly-once pair delivery under cancellation; sticky identity mismatch; and slow-provider slot skipping without rate interpolation. Final test/head/run results belong to PR #36 and must be checked after this commit; an earlier GREEN does not validate a later head.

## Direct review

The actual compare against main shows the old IncidentReviewForm changes only by a one-line link attachment, project metadata only by three version lines, and Program only by self-test wiring. New feature files contain the calculation, source, service, form/timeline/report and tests. Existing performance scheduler, machine telemetry, remediation worker/IPC/allow-list, UAC/context/Temp policy, health/coverage model, package dependencies and workflows remain unchanged.

The source opens QUERY_LIMITED_INFORMATION|SYNCHRONIZE and keeps one handle. No PROCESS_VM_READ/content access, process modification, token elevation, termination or artificial workload. Native layout matches PROCESS_MEMORY_COUNTERS_EX on x64; CPU/I/O are nullable deltas, memory fields remain independent. Initial identity comparison acknowledges WMI microsecond precision; later sampling keeps exact native creation time. Rates use actual elapsed intervals and full-system active processor count, not affinity or quotas. Independent host/process faults remain separate. HTML encodes text, exports are unique CreateNew files, both clocks share elapsed origin but retain source receipt times.

Review was performed directly by the implementing assistant; no independent reviewer/subagent or human approval of the implementation is claimed. Windows runtime evidence comes from existing hosted CI, not from the local Linux container. No Windows/.NET setup was attempted locally.

## Release gates and limits

After all final-head checks pass, merge only the expected protected head. Verify the exact main build, release publication, attestation generation and downloaded asset sizes/hashes/FileVersion separately; record them on PR #36. Existing v0.13.0 assets must not be overwritten. A PR or local build is not a release.

Corporate Windows11/RDP/alternate account/protected process, real application workloads, processor groups/CPU hotplug/affinity and visual DPI acceptance remain pilot work. Native integration queries its own process plus a disposable cmd fixture which exits normally when its standard input is released. It does not run repairs or terminate third-party processes. Chart painting and control construction tests are not screenshot/DPI acceptance. Cancellation is cooperative between system calls; no hard native deadline. Authenticode remains absent.

# CDC 2.3.3 Adoption Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Move G PC Health Check orchestration to CDC 2.3.3 without invalidating the pinned 0.17.0 pilot candidate.

**Architecture:** Vendor the exact CDC package, add a strict project adapter/checkpoint/runbooks, keep coordination state on a separate Git ref, and schedule an hourly condition watchdog. Product code and pinned pilot ref remain unchanged.

**Tech Stack:** GitHub, YAML/JSON control records, Python 3 + PyYAML validators, GitHub Actions, optional Codex COMPUTE_ONLY.

**Spec:** `docs/cdc-2.3-adoption.md`

## Global Constraints

- Do not merge/auto-merge PR #92 without explicit owner approval.
- Preserve `pilot/0.17.0-rc-f037bece` at `f037bece0272814f9b0f069aaf1de17be369b626`.
- Process-only CDC commits use `[skip ci]`; do not spend Actions just to validate policy.
- Codex stays disabled until environment/user/repository binding is actually verified.
- Compute wake cap 4; `status_polls:null`.

## Review Focus

- Accidental product/pilot SHA movement.
- Adapter/checkpoint policy digest mismatch.
- Timer-only lease takeover.
- Duplicate external submission after unknown outcome.
- Hosted/Codex evidence misrepresented as managed Windows 11 acceptance.

---

### Task 1: Vendor CDC core
**Files:** Create `.agents/skills/continuous-development-cycle/**`.
- [x] Copy complete 2.3.3 package and preserve VERSION/manifest/scripts/references/tests.

### Task 2: Add project policy and durable control plane
**Files:** Create `docs/development-cycle.yaml`, `docs/work-status/current.md`, coordination records.
- [x] Configure repository identity, Git lease backend and budget caps.
- [x] Bind checkpoint to semantic adapter digest and initialize released generation-0 lease.

### Task 3: Add runbooks and watchdog policy
**Files:** Create `docs/development-watchdog.md`, `docs/codex-compute.md`, `docs/cdc-2.3-adoption.md`; modify `AGENTS.md`.
- [x] Preserve project-specific Windows/security/release boundaries.

### Task 4: Strict validation and scheduler handoff
**Files:** Create `tools/validate_cdc_policy.py`.
- [ ] Run strict adapter/checkpoint/skill tests from a fresh checkout.
- [ ] Verify no product/workflow changes and pinned pilot ref unchanged.
- [ ] Create/read back hourly condition-watch automation.
